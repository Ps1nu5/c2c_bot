version: "3.9"

services:
  bot:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - bot_data:/app/data
    environment:
      - DATABASE_URL=sqlite+aiosqlite:///./data/bot.db

volumes:
  bot_data:
